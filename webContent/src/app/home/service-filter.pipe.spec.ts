import { ServiceFilterPipe } from './service-filter.pipe';

describe('ServiceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
